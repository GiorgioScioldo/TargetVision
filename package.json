{
  "name": "targetvision",
  "version": "1.0.0",
  "description": "TargetVision — rilevamento colpi sul bersaglio",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "build:android": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/filesystem": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
